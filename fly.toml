# fly.toml - Main configuration for Fly.io deployment
app = "infamous-freight-enterprises"
primary_region = "iad"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]
  # Use the root Dockerfile for full-stack deployment
  dockerfile = "Dockerfile"

[env]
  PORT = "3000"
  NODE_ENV = "production"

[build]
  dockerfile = "api/Dockerfile"

[env]
  PORT = "4000"
  NODE_ENV = "production"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1
  max_machines_running = 10
  processes = ["app"]

  [[http_service.checks]]
    grace_period = "10s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/"

  [http_service.concurrency]
    type = "requests"
    hard_limit = 250
    soft_limit = 200

[[vm]]
  cpu_kind = 'shared'
  cpus = 1

# Metrics and monitoring
[metrics]
  port = 9091
  path = "/metrics"

# Optional: Persistent storage for logs/temp files
# Uncomment and run: flyctl volumes create data --region iad --size 1
# [mounts]
#   source = "data"
#   destination = "/app/data"
