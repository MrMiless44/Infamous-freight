name: Release
description: Production deployment checklist
title: "[Release]: "

labels: ["release"]

body:

- type: markdown
  attributes:
    value: |
      Confirm the following before releasing.

- type: checkboxes
  id: release_checklist
  attributes:
    label: Checklist
    options:

      - label: CI is green
      - label: Tests passing
      - label: Security scan clean
      - label: Version updated
      - label: Deployment plan ready
