engine-strict=false

# Netlify installs UI-managed build plugins in apps/web/.netlify/plugins.
# Some third-party plugin engine ranges lag behind the current build runtime.
# Keep strict engines at repo root, but relax it for the web package here.
# Plugin-specific install behavior (including lifecycle script suppression)
# should remain scoped to apps/web/.netlify/plugins/.npmrc.
